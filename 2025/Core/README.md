Core Workshops
==============

Models in here are used by the core _Workshops_ from the _FlexSim Field Guide_ (`FLX). Each number should correspond to a chapter in the Field Guide, and usually includes the templates for that Workshop and an example solution.